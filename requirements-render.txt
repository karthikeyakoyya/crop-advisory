# Minimal dependencies to SERVE the trained model via the API.
# Training-only tools (mlflow, pycaret) are intentionally excluded here to
# keep deployment fast and avoid their heavier/less-stable transitive
# dependencies. For local training, use the full requirements.txt instead.
# Versions pinned to what was already confirmed working on Python 3.11.
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
joblib==1.5.3
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
python-dotenv==1.2.2
fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
