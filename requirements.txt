pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
mlflow>=2.10
joblib>=1.3
sqlalchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# Optional AutoML baseline (heavier install; pipeline degrades gracefully
# without it — see models/train.py). Comment out if you want a lighter
# install and are fine skipping the AutoML baseline sweep.
pycaret>=3.2
